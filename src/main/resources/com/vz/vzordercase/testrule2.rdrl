package com.vz.vzordercase;

import java.lang.Number;

rule "testrule2"
	dialect "mvel"
	ruleflow-group "processSelection"
	when
		not (Order( )) 
	then
		System.out.println( "Rule fired" + kcontext.rule.name );
end
